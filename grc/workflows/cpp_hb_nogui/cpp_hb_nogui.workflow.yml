id: cpp_hb_nogui_workflow
description: "c++ hier block no gui workflow"
output_language: cpp
output_language_label: C++
generator_module: gnuradio.grc.workflows.cpp_hb_nogui
generator_class: CppHierBlockGenerator
generator_options: hb
generator_options_label: Hier Block
parameters:
-   id: gen_cmake
    label: Generate CMakeLists.txt
    dtype: enum
    default: 'On'
    options: ['On', 'Off']
    # hide: 'part'
    hide: ${ ('part' if output_language == 'cpp' else 'all') }
-   id: cmake_opt
    label: CMake options
    dtype: string
    default: ''
    # hide: 'part'
    hide: ${ ('part' if output_language == 'cpp' else 'all') }
-   id: category
    label: Category
    dtype: string
    default: '[GRC Hier Blocks]'
    # hide: 'none'
    hide: ${ ('none' if generate_options.startswith('hb') else 'all') }
