id: python_hb_nogui_workflow
description: "python hier block no gui workflow"
output_language: python
output_language_label: Python
generator_module: gnuradio_companion.workflows.python_hb_nogui
generator_class: HierBlockGenerator
generator_options: hb
generator_options_label: Hier Block
parameters:
-   id: category
    label: Category
    dtype: string
    default: '[GRC Hier Blocks]'
    hide: 'none'
templates:
    imports: |-
        from gnuradio import gr
        from gnuradio.filter import firdes
        from gnuradio.fft import window
        import sys
        import signal
        % if generate_options == 'qt_gui':
        from PyQt5 import Qt
        % endif
        % if not generate_options.startswith('hb'):
        from argparse import ArgumentParser
        from gnuradio.eng_arg import eng_float, intx
        from gnuradio import eng_notation
        % endif
